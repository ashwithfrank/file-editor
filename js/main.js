// main.js
// Entry point: just bootstraps the UI once the DOM is ready. All real
// logic lives in state.js (data), editor.js (the editing surface), and
// ui.js (everything else).

import { initUI } from './ui.js';

initUI();
